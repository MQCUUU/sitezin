'use client'; import {useEffect,useState} from 'react'; import {Search} from '@/components/Search'; import {PosterGrid} from '@/components/PosterGrid'; import type {LibraryItem} from '@/lib/types'; export default function Favorites(){const[d,setD]=useState<LibraryItem[]>([]);useEffect(()=>{fetch('/api/library').then(r=>r.json()).then(x=>setD(Array.isArray(x)?x.map((i:any)=>({...i,library_id:i.id,...i.media})).filter((i:any)=>i.favorite):[]))},[]);return <><Search/><div className="section"><div className="eyebrow">O que você mais gosta</div><h1>Favoritos</h1></div><PosterGrid items={d}/></>}
