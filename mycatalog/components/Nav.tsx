'use client'; import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {House,Library,ChartNoAxesCombined,Heart,Settings,LogOut,Trophy} from 'lucide-react'; import {createClient} from '@/lib/supabase/client';
const items=[['/','Início',House],['/library','Biblioteca',Library],['/ranking','Meu Ranking',Trophy],['/stats','Estatísticas',ChartNoAxesCombined],['/favorites','Favoritos',Heart],['/settings','Configurações',Settings]] as const;
export function Nav(){const p=usePathname(); const signout=async()=>{await createClient().auth.signOut();location.href='/'}; return <><aside className="sidebar"><div className="brand">My<span>Catalog</span></div><nav className="nav">{items.map(([href,label,Icon])=><Link key={href} href={href} className={p===href?'active':''}><Icon size={18}/>{label}</Link>)}</nav><button className="btn ghost" style={{marginTop:20,width:'100%',textAlign:'left'}} onClick={signout}><LogOut size={18}/> Sair</button></aside><nav className="mobile-nav">{items.slice(0,5).map(([href,label,Icon])=><Link key={href} href={href} className={p===href?'active':''}><Icon size={18}/>{label}</Link>)}</nav></>}
