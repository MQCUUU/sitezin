import Link from 'next/link'; import {Heart} from 'lucide-react'; import {img} from '@/lib/tmdb'; import type {LibraryItem} from '@/lib/types';
export function PosterGrid({items}:{items:LibraryItem[]}){if(!items.length)return <div className="empty">Nenhum título encontrado. Use a busca para adicionar filmes e séries.</div>;return <div className="grid">{items.map(x=><Link href={`/title/${x.media_type}/${x.tmdb_id}`} className="card" key={x.library_id}><div className="poster"><img src={img(x.poster_path)}/><span className="badge">{x.media_type==='tv'?'SÉRIE':'FILME'}</span><span className={x.favorite?'heart on':'heart'}><Heart size={16} fill={x.favorite?'currentColor':'none'}/></span></div><div className="card-title">{x.title}</div><div className="card-meta">{x.personal_rating!==null?<span className="rating">★ {x.personal_rating.toFixed(1)}</span>:x.status}</div></Link>)}</div>}
