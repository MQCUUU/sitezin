import {NextResponse} from 'next/server'; import {createClient} from '@/lib/supabase/server';
export async function GET(){const s=await createClient(); const {data:{user}}=await s.auth.getUser(); if(!user)return NextResponse.json({error:'Não autenticado'},{status:401}); const {data,error}=await s.from('library_items').select('id,status,favorite,personal_rating,review,watched_at,rewatch_count,added_at,updated_at,media:media_id(*)').eq('user_id',user.id).order('added_at',{ascending:false}); if(error)return NextResponse.json({error:error.message},{status:500}); return NextResponse.json(data)}
export async function POST(req:Request){const s=await createClient(); const {data:{user}}=await s.auth.getUser(); if(!user)return NextResponse.json({error:'Não autenticado'},{status:401}); const b=await req.json(); const media=b.media; const {data:m,error:me}=await s.from('media').upsert({tmdb_id:media.id,media_type:media.media_type,title:media.title,original_title:media.original_title,overview:media.overview,poster_path:media.poster_path,backdrop_path:media.backdrop_path,release_date:media.release_date||null,first_air_date:media.first_air_date||null,genres:media.genres||[],tmdb_rating:media.vote_average||media.tmdb_rating||null,tmdb_vote_count:media.vote_count||null,runtime:media.runtime||null,seasons_count:media.number_of_seasons||null,episodes_count:media.number_of_episodes||null,creator_names:media.creator_names||[],cast_names:media.cast_names||[],raw:media},{onConflict:'tmdb_id,media_type'}).select().single(); if(me)return NextResponse.json({error:me.message},{status:500}); const {data:item,error:ie}=await s.from('library_items').upsert({user_id:user.id,media_id:m.id,status:b.status||'want',favorite:!!b.favorite,personal_rating:b.personal_rating??null},{onConflict:'user_id,media_id'}).select('id,status,favorite,personal_rating,review,watched_at,rewatch_count,added_at,updated_at,media:media_id(*)').single(); if(ie)return NextResponse.json({error:ie.message},{status:500}); return NextResponse.json(item)}
