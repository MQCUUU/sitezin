'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client';
export default function Auth(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[mode,setMode]=useState<'login'|'signup'>('login');const[msg,setMsg]=useState('');const go=async()=>{const s=createClient();const r=mode==='login'?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password});setMsg(r.error?.message||'Confira seu e-mail ou entre no catálogo.');if(!r.error)location.href='/'};return <main style={{maxWidth:440,margin:'10vh auto'}}><div className="brand">My<span>Catalog</span></div><div className="panel"><h1>{mode==='login'?'Entrar':'Criar conta'}</h1><div className="form"><input placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn primary" onClick={go}>{mode==='login'?'Entrar':'Criar conta'}</button></div>{msg&&<p className="muted">{msg}</p>}<button className="btn ghost" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Ainda não tenho conta':'Já tenho conta'}</button></div></main>}
